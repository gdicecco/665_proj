model{
    for(i in 1:n){
    y[i] ~ dpois(lambda[i])
    lambda[i] <- exp(b1*X[i, 2] + b2*X[i, 3] + b3*X[i, 5] + a1[ix[i]]*X[i, 4])
    }
    
    for(j in 1:nobs){
      a1[j] ~ dnorm(0.0, sigma)
    }
    
    sigma ~ dgamma(0.001, 1000)
    b1 ~ dnorm(0.0, 1000)
    b2 ~ dnorm(0.0, 1000)
    b3 ~ dnorm(0.0, 1000)
    }
    
